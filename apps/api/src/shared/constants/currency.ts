// Kept in lockstep with apps/desktop and apps/web's own config/branding.ts
// currencySymbol -- this is server-side text (Activity Log summaries), which
// can't import a frontend config module across the app boundary.
export const CURRENCY_SYMBOL = 'Rs. ';
